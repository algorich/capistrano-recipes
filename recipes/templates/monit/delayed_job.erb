check process delayed_job_<%= application %> with pidfile <%= delayed_job_pid %>
  start program = "/etc/init.d/delayed_job_<%= application %> start"
  stop program = "/etc/init.d/delayed_job_<%= application %> stop"
  restart program = "/etc/init.d/delayed_job_<%= application %> restart"
