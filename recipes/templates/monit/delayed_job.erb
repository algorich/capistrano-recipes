check process delayed_job
  with pidfile <%= "#{shared_path}/pids/delayed_job.pid" %>
  start program = "/etc/init.d/delayed_job_<%= application %> start"
  stop program = "/etc/init.d/delayed_job_<%= application %> stop"
